# 品牌图标源文件

`logo.svg`、`foreground.svg` 与 `monochrome.svg` 是唯一可编辑源文件，画布和 `viewBox` 均为 1024 × 1024。

- 中心圆形 Surface 直径占画布 50%，位于 Adaptive Icon 安全区域内。
- 两条轻量曲线抽象出书页，一道垂直线形成进入阅读空间的光线；符号宽度约占圆形 42%。
- Android `drawable` 中的 VectorDrawable 是用于 Adaptive Icon 和 Android 13 单色图标的派生格式。
